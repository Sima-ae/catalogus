# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Stale nested clone on VPS (must not live in repo)
catalogus/

# Next.js
.next/
out/
build/
dist/

# Production
/build

# Local dev persistence (settings when DB offline)
.data/
.cache/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files (never commit secrets)
.env
.env.*
!.env*.example
!.env.local.example
!.env.vps.example

# Site access codes seed (private; keep local/untracked only)
db/site_access_codes_seed.txt

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo
next-env.d.ts

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Product images (imports + uploads) live in public/images and are deployed via git.
